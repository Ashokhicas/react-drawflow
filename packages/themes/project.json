{
  "name": "themes",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/themes/src",
  "projectType": "library",
  "tags": ["scope:themes", "type:lib"],
  "targets": {
    "type-check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit",
        "cwd": "packages/themes"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}

